import React from 'react';
import { StyledProps } from 'appProps';

export class Dashboard extends React.Component<StyledProps> {
    render() {
        return (
            <div>
                Dashboard
            </div>
        );
    }
}

export default Dashboard;